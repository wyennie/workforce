"""Package version for Workforce.

``setuptools`` reads ``__version__`` from this module via dynamic versioning
so it never needs to be duplicated in ``pyproject.toml``.
"""

__version__ = "0.1.0.dev0"
